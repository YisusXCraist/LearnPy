### Tuples ###

my_tuple = tuple() #tuple() creates an empty tuple

my_other_list = ()  #() creates an empty tuple

my_tuple = (28, 1.65, "Yisus", "xCraist") #tuple() creates a tuple with the elements specified